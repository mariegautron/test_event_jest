
class EventForPay {

    constructor(events) {
      // Well in its current form EventForPay doesn't provide value yet
      // So I woudn't have coded it this way yet. I would have just returned an array of events.
      this.events = events
    }
  }

  module.exports = EventForPay;